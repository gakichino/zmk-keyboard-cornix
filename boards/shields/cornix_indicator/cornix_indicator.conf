## use ws2812-spi
# Zephyr 4.1 では WS2812_STRIP は廃止され、DT の compatible="worldsemi,ws2812-spi"
# から WS2812_STRIP_SPI が自動的に有効化される。旧 CONFIG_WS2812_STRIP は指定しない。
CONFIG_SPI=y
CONFIG_LED_STRIP=y
CONFIG_ZMK_EXT_POWER=y

CONFIG_RGBLED_WIDGET=y
CONFIG_RGBLED_WIDGET_WS2812=y
CONFIG_RGBLED_WIDGET_LED_COUNT=2

CONFIG_RGBLED_WIDGET_BRIGHTNESS=64

CONFIG_RGBLED_WIDGET_LED_SHARING=y
